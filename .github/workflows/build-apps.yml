name: Flutter Deployment

on:
  push:
    branches:
      - main

jobs:
  windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
        with:
          channel: "stable"
      - name: Install project dependencies
        run: flutter pub get
      - name: Generate intermediates
        run: flutter pub run build_runner build --delete-conflicting-outputs
      - name: Build artifacts
        run: dart run msix:create -d "AssignGo" -i "IshanMisra.AssignGo" -n "AssignGo"
      - name: Release msix to github
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{github.ref_name}}
          files: build/windows/x64/runner/Release/AssignGo.msix
      - name: Build appbundle for Android
        run: flutter build appbundle --split-debug-info=./debug-info --obfuscate --release
      - name: Package Android Release
        uses: thedoctor0/zip-release@master
        with:
          type: "zip"
          filename: local_api_server-${{github.ref_name}}-android.zip
          directory: build/app/outputs/bundle/release
      - name: Release app bundle to github
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{github.ref_name}}
          files: build/app/outputs/bundle/release/local_api_server-${{github.ref_name}}-android.zip

  macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
        with:
          channel: "stable"
      - name: Install project dependencies
        run: flutter pub get
      - name: Generate intermediates
        run: flutter pub run build_runner build --delete-conflicting-outputs
      - name: Build artifacts
        run: flutter build macos --split-debug-info=./debug-info --obfuscate --release
      - name: Package Release MacOS
        uses: thedoctor0/zip-release@master
        with:
          type: "zip"
          filename: local_api_server-${{github.ref_name}}-macos.zip
          directory: build/macos/Build/Products/Release
      - name: Release MacOS to github
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{github.ref_name}}
          files: build/macos/Build/Products/Release/local_api_server-${{github.ref_name}}-macos.zip
      - name: Package Release iOS IPA
        uses: thedoctor0/zip-release@master
        with:
          type: "zip"
          filename: local_api_server-${{github.ref_name}}-ios.zip
          directory: build/ios/ipa
      - name: Release IPA to github
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{github.ref_name}}
          files: build/ios/ipa/local_api_server-${{github.ref_name}}-ios.zip
